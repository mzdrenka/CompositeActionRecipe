name: 'Composite Action Recipe Mateusz Zdrenka'
description: 'Greets a user and returns the answer to life, the universe and everything (42)'
branding:
  icon: anchor
  color: purple
author: Mateusz Zdrenka

inputs:
  who-to-greet:
    description: 'Name or handle of the person to greet'
    required: true
    default: 'Świat'
outputs:
  answer:
    description: "The answer to life, the universe, and everything"
    value: ${{ steps.greet.outputs.answer }}
runs:
  using: 'composite'
  steps:
    - id: greet
      name: Greet user and set output
      uses: actions/github-script@v6
      with:
        script: |
          const whoToGreet = core.getInput('who-to-greet')
          core.notice(`Hello ${whoToGreet}`)
          core.setOutput('answer', 42)

          if (context.eventName === 'issues' && context.payload.action === 'opened') {
           await github.rest.issues.createComment({
             issue_number: context.issue.number,
             owner: context.repo.owner,
             repo: context.repo.repo,
             body: 'Thanks for reporting!'
           })
          }
